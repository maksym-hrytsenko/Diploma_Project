# Копія для перевірки (українською)

Дослівний переклад щойно написаних тест-кейс-документів (англійською, у `tests/`). Після перевірки — секцію можна стирати.

---

## mode_entry_test_scenarios.md (нове, повний текст)

**Тест-сценарії входу/виходу з режимів (усі способи вводу)**

Кожен режим системи (Presentation, Flip, Cursor, Call) можна активувати аж чотирма незалежними шляхами — голосом, клавіатурою, UI-колесом керування, і (для чотирьох звичайних режимів) опосередковано через жестове меню Quick Circle — а вийти з режиму можна кількома незалежними шляхами теж. Кожен шлях — це окрема гілка коду в `SignalMapper` (`_update_mode`/`_mode_trigger_matches`), тож правильного розпізнавання жестів режиму недостатньо — потрібно перевірити кожен спосіб *потрапити* в цей режим. Цей документ існує саме для цього; документи про жести й обличчя вже припускають, що режим активний.

Оцінка: **бінарна** (pass/fail) — правильний режим входить/виходить, і крапка. Часткової оцінки для переходу режиму не буває.

### 1. Presentation — кожен спосіб входу

| # | Спосіб входу | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Голос, рівень 1 (точний) | Сказати "jack presentation mode" | Активний Presentation |
| 2 | Голос, рівень 2 (семантика) | Сказати "jack switch to presentation mode" | Активний Presentation |
| 3 | Голос, рівень 3 (LLM) | Сказати "jack I'm about to give a talk, could you switch me into presentation mode" | Активний Presentation |
| 4 | Клавіатура | Натиснути `ctrl+shift+p` | Активний Presentation |
| 5 | UI | Клікнути іконку Presentation на колесі керування | Активний Presentation |
| 6 | Жест (Quick Circle) | З бездіяльності: кулак → розкрита долоня, свайп ліворуч | Активний Presentation |

### 2. Flip — кожен спосіб входу

| # | Спосіб входу | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Голос, рівень 1 | "jack flip mode" | Активний Flip |
| 2 | Голос, рівень 2 | "jack switch to flip mode" | Активний Flip |
| 3 | Голос, рівень 3 | "jack I want to scroll through pages, can you put me in flip mode please" | Активний Flip |
| 4 | Клавіатура | `ctrl+shift+f` | Активний Flip |
| 5 | UI | Іконка Flip на колесі | Активний Flip |
| 6 | Жест (Quick Circle) | Кулак → долоня, свайп вгору | Активний Flip |

### 3. Cursor — кожен спосіб входу

| # | Спосіб входу | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Голос, рівень 1 | "jack cursor mode" | Активний Cursor |
| 2 | Голос, рівень 2 | "jack switch to cursor mode" | Активний Cursor |
| 3 | Голос, рівень 3 | "jack I need to control the mouse with my hand, switch to cursor mode please" | Активний Cursor |
| 4 | Клавіатура | `ctrl+shift+c` | Активний Cursor |
| 5 | UI | Іконка Cursor на колесі | Активний Cursor |
| 6 | Жест (Quick Circle) | Кулак → долоня, свайп вниз | Активний Cursor |

### 4. Call — кожен спосіб входу

| # | Спосіб входу | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Голос, рівень 1 | "jack call mode" | Активний Call |
| 2 | Голос, рівень 2 | "jack switch to call mode" | Активний Call |
| 3 | Голос, рівень 3 | "jack I'm joining a meeting, could you put me into call mode" | Активний Call |
| 4 | Клавіатура | `ctrl+shift+w` | Активний Call |
| 5 | UI | Іконка Call на колесі | Активний Call |
| 6 | Жест (Quick Circle) | Кулак → долоня, свайп праворуч | Активний Call |

### 5. Вихід з активного режиму — кожен спосіб виходу

Увійти в будь-який режим, потім спробувати кожен спосіб виходу по черзі (повертаючись у режим між спробами):

| # | Спосіб виходу | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Голос | "jack exit mode" | Повернення в бездіяльність |
| 2 | Голос, рівень 3 | "jack could you please take me out of this mode" | Повернення в бездіяльність |
| 3 | Клавіатура | `Esc` | Повернення в бездіяльність |
| 4 | UI | Клік по тій самій іконці режиму на колесі ще раз (перемкнути назад) | Повернення в бездіяльність |
| 5 | Жест, лише для Quick Circle | Поки Quick Circle відкрите: кулак → долоня → знову кулак, без свайпу | Quick Circle закривається без входу в режим (це власний вихід Quick Circle; стискання кулака посеред свайпу в Flip/Presentation/Cursor/Call **не** виводить з цих режимів) |

### 6. Пряме перемикання режим-у-режим (без явного виходу)

| # | Кроки | Очікуваний результат |
|---|---|---|
| 1 | Активний Flip, сказати "jack cursor mode" напряму (без "exit mode" спочатку) | Перемикається напряму в Cursor — старий режим виходить внутрішньо перед входом у новий, без потреби виходити спочатку |
| 2 | Активний Cursor, клікнути іконку Call напряму | Перемикається напряму в Call |
| 3 | Спробувати перемкнутись у той самий режим (напр. сказати "jack flip mode", вже перебуваючи у Flip) | Нічого не відбувається — лишається Flip, без циклу вихід/повторний вхід, без дублікату події mode_changed |

### 7. Взаємодія з System OFF

| # | Кроки | Очікуваний результат |
|---|---|---|
| 1 | Вимкнути систему (кнопка хаба в UI) у бездіяльності, потім спробувати будь-який спосіб входу в будь-який режим | Нічого не відбувається — жоден режим не входить, поки система вимкнена |
| 2 | Вимкнути систему, поки режим активний | Режим візуально виходить як частина вимкнення (див. також `stress_test_scenarios.md` §5) |
| 3 | При вимкненій системі клікнути іконку режиму на колесі | Система автоматично вмикається як частина цього кліку, потім запитаний режим входить — UI ніколи не показує активний режим, поки хаб все ще показує OFF |

### Як тестувати

1. Запустити `python src/main.py --debug-voice` (додати `--debug-gesture`, якщо тестуєте і вхід через Quick Circle в тому самому прогоні).
2. Для кожного режиму пройти всі шість рядків входу, підтверджуючи через рядок логу `Mode -> <назва>` і світіння/підпис UI, а не лише візуально.
3. Занотувати pass/fail для кожного рядка. Фраза рівня 2/3, яка "провалюється" до "команду не зрозуміло", — це fail саме для цього рядка, навіть якщо рівень 1 того самого режиму пройшов — занотуйте, який саме рівень зламався.

---

## environment_test_scenarios.md (нове, повний текст)

**Тест-сценарії середовищ**

Ручні тест-сценарії для п'яти голосових середовищ (Work, Job Search, Study, Movie, News), визначених у `config/fusion.json`. На відміну від режимів, середовища довготриваліші й мають реальні побічні ефекти в системі (відкриття застосунків, перемикання "Не турбувати", заборона сну екрана) — `test_command_pipeline.py` вже перевіряє *логіку рішень* (правильні `enter_actions`/`exit_actions` спрацьовують у правильному порядку) проти замокованого `OSController`, тож ці сценарії існують, щоб перевірити **реальні** ефекти на рівні системи, яких замокована автоматизована сюїта не бачить.

Оцінка: **бінарна** (pass/fail) для кожного реального ефекту.

### 1. Середовище Work
1. Сказати "jack work mode" → "Не турбувати" вмикається; Slack, Пошта і Календар справді відкриваються.

### 2. Середовище Job Search
1. Сказати "jack job search mode" → "Не турбувати" вмикається; вікна браузера для пошуку роботи справді відкриваються.

### 3. Середовище Study
1. Сказати "jack study mode" → "Не турбувати" вмикається; навчальні вікна браузера справді відкриваються.

### 4. Середовище Movie
1. Сказати "jack movie mode" → "Не турбувати" вмикається; сон екрана заборонено (екран не тьмяніє/блокується під час перегляду); TV.app і Netflix справді відкриваються; сценарій "кінематографічного режиму" в Shortcuts (розумне світло) справді запускається.

### 5. Середовище News
1. Сказати "jack news mode" → новинні вкладки браузера справді відкриваються (без зміни "Не турбувати" — у News немає такого enter_actions).

### 6. Вихід із середовища
1. З Work сказати "jack exit mode" → "Не турбувати" вимикається знову (exit_actions Work).
2. З Movie вийти → "Не турбувати" вимикається І заборона сну екрана знімається (обидва exit_actions Movie).
3. З News вийти → нічого не відбувається — у News порожній список exit_actions, це коректно, не баг.

### 7. Пряме перемикання між середовищами
1. Активне Work, сказати "jack movie mode" напряму → спочатку виконуються exit_actions Work ("Не турбувати" вимк.), потім enter_actions Movie — перевірити за реальним станом DND і вікнами застосунків, не лише за логом.
2. Перемкнутись у те саме середовище, в якому вже перебуваєте → без дублікату циклу вхід/вихід.

### 8. Незалежність середовища й режиму
1. Увійти в середовище Work, потім увійти в режим Flip поверх нього → обидва активні одночасно; жести Flip працюють нормально.
2. Вийти з режиму Flip (голос/Esc/UI), поки Work ще активне → виходить лише режим — застосунки/DND-стан Work не зачіпаються.
3. Вийти із середовища Work, поки режим активний → розгортається лише стан середовища; активний режим не зачіпається.

### Як тестувати
1. Запустити `python src/main.py --debug-voice`.
2. Перед кожним тестом середовища закрити застосунки, які воно відкриє, і переконатись, що "Не турбувати" вимкнено, щоб дійсно спостерігати зміну.
3. Для Movie — реально залишити екран в бездіяльності довше за звичайний тайм-аут сну, щоб підтвердити, що заборона сну реальна, а не лише в лозі.
4. Занотувати pass/fail для кожного рядка, з приміткою, який саме застосунок чи налаштування системи не змінилось, якщо рядок провалився.

---

## global_functions_test_scenarios.md (нове, повний текст)

**Тест-сценарії глобальних функцій (запуск застосунків, керування медіа)**

Ці голосові функції працюють однаково незалежно від режиму — вони завжди доступні, не прив'язані до Presentation/Flip/Cursor/Call. `tests/voice_test_phrases.md` вже вичерпно покриває всі три рівні розпізнавання (точний/семантичний/LLM) для кожної команди нижче; цей документ натомість перевіряє, що розпізнана команда дає правильний **реальний** ефект у системі — те, чого автоматизована регресна сюїта не бачить, бо там `OSController` замокований.

Оцінка: **бінарна** (pass/fail).

### 1. Запуск застосунків (усі 19)
Сказати "jack open &lt;застосунок&gt;" для кожного рядка (фразування рівня 1 — покриття рівнів вже покрито `voice_test_phrases.md`).

| # | Команда | Очікуваний реальний ефект |
|---|---|---|
| 1 | "jack open browser" | Відкривається браузер за замовчуванням на google.com |
| 2 | "jack open chatgpt" | Відкривається chatgpt.com |
| 3 | "jack open github" | Відкривається github.com |
| 4 | "jack open vscode" | Відкривається VS Code (потребує CLI `code` у PATH) |
| 5 | "jack open terminal" | Відкривається Термінал |
| 6 | "jack open safari" | Відкривається Safari |
| 7 | "jack open chrome" | Відкривається Google Chrome |
| 8 | "jack open spotify" | Відкривається Spotify |
| 9 | "jack open slack" | Відкривається Slack |
| 10 | "jack open discord" | Відкривається Discord |
| 11 | "jack open mail" | Відкривається Пошта |
| 12 | "jack open calendar" | Відкривається Календар |
| 13 | "jack open notes" | Відкриваються Нотатки |
| 14 | "jack open telegram" | Відкривається Telegram |
| 15 | "jack open finder" | Відкривається вікно Finder |
| 16 | "jack open notion" | Відкривається Notion |
| 17 | "jack open photos" | Відкривається Фото |
| 18 | "jack open preview" | Відкривається Перегляд |
| 19 | "jack open settings" | Відкриваються Системні налаштування |

### 2. Глобальні медіа-команди
Спочатку щось увімкнути (трек у Spotify чи вкладка YouTube у звичайному браузері — не screen share, див. §3).

| # | Команда | Очікуваний результат |
|---|---|---|
| 1 | "jack start" | Плей/пауза перемикається |
| 2 | "jack stop" | Плей/пауза перемикається (той самий тумблер, що й "start" — **не** зупиняє/перемотує відтворення, бо STOP/PAUSE/RESET усі мапляться на ту саму дію MEDIA_PLAY_PAUSE) |
| 3 | "jack pause" | Плей/пауза перемикається |
| 4 | "jack reset" | Плей/пауза перемикається — переконайтесь, що трек НЕ перезапускається з початку |
| 5 | "jack next track" | Перехід на наступний трек |
| 6 | "jack previous track" | Перехід на попередній трек |

### 3. Медіа-команди проти screen-share / вбудованого відео (відоме обмеження)
1. Відео у звичайній вкладці Safari/Chrome, сказати "jack next track" → трек справді перемикається.
2. Та сама вкладка через screen share відеодзвінка (напр. режим Zoom "optimize for video clip"), сказати "jack next track" → у консолі все одно друкується `[EXECUTOR] NEXT_TRACK`, але відео в шарінгу НЕ перемикається — це застосунок "ковтає" медіа-клавішу без помилки (відоме обмеження платформи, не баг системи), а не тиха відмова розпізнавання.

### 4. Face-layer медіа-комбо (перехресне посилання)
Уже покрито в `face_test_scenarios.md` §1–§3 (нахил голови, відкриття рота, подвійне моргання з Alt) — повторювати тут лише якщо хочете один комбінований прогін усіх шляхів керування медіа (голос + обличчя) за раз; нових сценаріїв понад той документ немає.

### Як тестувати
1. Запустити `python src/main.py --debug-voice`.
2. Для §1 закривати кожен застосунок спочатку, щоб відкриття було дійсно спостережуваним, і перевіряти конкретне вікно/URL, а не просто "щось відкрилось".
3. Для §2–§3 мати реально відтворюване медіа перед командою — тиша робить перемикання невідрізнюваним від бездіяльності.
4. Занотувати pass/fail для кожного рядка.

---

## ui_os_integration_test_scenarios.md (нове, повний текст)

**Тест-сценарії UI та інтеграції з macOS**

Ручні сценарії для десктопного UI (`MainWindow`, `FloatingStatusBar`, `PointerOverlay`, `QuickCommandOverlay`) і специфічної для macOS поведінки вікон (`native_window.py`). Вхід у режими через UI вже покрито в `mode_entry_test_scenarios.md`; цей документ покриває все інше, за що відповідає UI й шар інтеграції з ОС.

Оцінка: **бінарна** (pass/fail).

### 1. Перемикачі модулів (нижня статус-панель)
1. Вимкнути перемикач камери → прев'ю показує заглушку; жести з камери перестають працювати.
2. Увімкнути назад → живе прев'ю відновлюється, жести знову працюють, без перезапуску.
3. Вимкнути перемикач мікрофона → голосові команди перестають розпізнаватись.
4. Увімкнути назад → голосові команди знову працюють.
5. Вимкнути перемикач клавіатури → клавіатурні комбінації (вхід у режим, face-layer Alt/Ctrl, Esc) перестають працювати.
6. Увімкнути назад → клавіатурні комбінації знову працюють.

### 2. Кнопка System ON/OFF
Див. `stress_test_scenarios.md` §5 для edge-case взаємодії з режимом. Тут — прості випадки:
1. Клік по хабу, вимкнути систему в бездіяльності → хаб тьмяніє/показує OFF; жодна голосова/жестова/клавіатурна команда не діє.
2. Клік по хабу, увімкнути знову → нормальна робота одразу відновлюється.

### 3. Вікна Settings і Info
1. Клік по іконці шестерні в заголовку → відкривається вікно Settings.
2. Клік ще раз, поки вікно вже відкрите → існуюче вікно підіймається/фокусується, не дублюється.
3. Клік по "Functions description" → відкривається вікно Info.
4. Клік ще раз, поки вже відкрите → підіймається/фокусується, не дублюється.

### 4. Minimize-to-bar / плаваюча статус-панель
1. Клік "Minimize to bar" → MainWindow ховається; з'являється маленька FloatingStatusBar угорі праворуч, що відображає поточний стан модулів/режиму (затемнені іконки для вимкнених модулів, підсвічений обідок на активному режимі, якщо є).
2. Поки панель видима, перемкнути модуль чи змінити режим голосом/жестом → іконки панелі оновлюються відповідно, без потреби відкривати MainWindow.
3. Клік по стрілці розгортання на панелі → MainWindow з'являється знову; панель ховається.

### 5. Хром вікна
1. Перетягнути заголовок мишею → вікно рухається за курсором.
2. Клік по кнопці згортання в заголовку → вікно згортається в Dock.
3. Клік по кнопці закриття (✕) в заголовку → застосунок повністю завершується — камера, мікрофон, розпізнавання жестів/голосу і будь-який оверлей зупиняються, а не лише закривається вікно.

### 6. Прапорці налагодження
1. Запуск з `--debug-gesture` → з'являється живий оверлей калібрування жестів.
2. Запуск з `--debug-face` → з'являється окреме живе вікно калібрування обличчя (pitch/yaw/roll, смужки blendshape з позначками порогів).
3. Запуск з `--debug-voice` → консоль друкує детальну інформацію розпізнавання: часткові фрази, фінальний результат, який рівень його розв'язав (або що жоден не розв'язав).
4. Запуск з кількома прапорцями одночасно (напр. `--debug-gesture --debug-face`) → обидва вікна відкриваються разом без конфлікту.

### 7. Поведінка Dock, Spaces і повноекранного режиму macOS
1. Перевірити Dock і Cmd+Tab, поки застосунок (включно з MainWindow) працює → жодної окремої іконки в Dock чи записі в Cmd+Tab — застосунок навмисно працює як фонова допоміжна програма.
2. Показати лазерну точку (`Pointing_Up`, без активного режиму) або відкрити Quick Circle, потім перемкнутись на інший Space → оверлей лишається видимим і на новому Space.
3. Показати той самий оверлей, поки інший застосунок у повноекранному режимі → оверлей все одно рендериться поверх повноекранного застосунку.
4. Клікнути в інший застосунок (забравши фокус), потім викликати оверлей → оверлей все одно з'являється, хоча застосунок не фронтмост.
5. Перемкнути Space, поки відкрите саме MainWindow (не оверлей) → MainWindow НЕ слідує за вами — це звичайне вікно, залишається на своєму Space, на відміну від оверлеїв вище.

### 8. Лазерна вказівка і зовнішній дисплей (проектор)
1. Підключити другий дисплей (проектор) ДО запуску застосунку, показати `Pointing_Up` без активного режиму → точка з'являється на зовнішньому дисплеї, не на екрані ноутбука.
2. Лише один дисплей → точка на основному екрані.
3. Підключити другий дисплей ПІСЛЯ запуску, показати `Pointing_Up` → точка все одно на початковому екрані — цільовий дисплей обирається лише один раз, при старті; потрібен перезапуск, щоб підхопити новий дисплей.

### 9. Перевірка консолі/терміналу (фінальний прогін)
Пройти репрезентативну вибірку сценаріїв з інших документів ще раз, цього разу спостерігаючи лише вивід терміналу:
1. Виконати будь-яку штатну дію → з'являється рівно один рядок `[EXECUTOR] <КОМАНДА>`, без дублікатів.
2. Сказати команду без слова-активатора "jack" спочатку → жодного рядка команди не з'являється — сесія не була відкрита.
3. Виконати жест/голос, що не належить поточному режиму → жодного рядка команди.
4. Сказати після "jack" фразу, яка нічому не відповідає на жодному рівні → зрештою з'являється рядок на кшталт `[RESOLVED] not understood: ...` після тайм-ауту сесії.
5. Клікнути кнопки UI напряму (колесо режимів, перемикачі, System ON/OFF) → ці дії **не** друкують рядки `[EXECUTOR] ...` самі по собі — вони публікують події `ui_*`, які йдуть через той самий конвеєр, що й будь-яке інше джерело, тож друкується результат (якщо є), а не сам клік.
6. Пройти повну сесію, торкнувшись кожного режиму/середовища/глобальної команди хоча б раз → у лозі ніде не з'являється рядок з позначкою `ERROR`.

### Як тестувати
1. Запустити `python src/main.py` (прапорці налагодження за §6 за потреби).
2. Пройти кожен розділ, порівнюючи поведінку на екрані зі стовпцем "Очікуваний результат".
3. Занотувати pass/fail для кожного рядка, з рядками логу для всього неочікуваного.

---

## Доповнення до stress_test_scenarios.md (нові розділи 11–16)

### 11. Межовий час: дебаунс підтвердження жесту
Утримати статичний жест (напр. Closed_Fist) лише 2-3 кадри, потім розслабити → це має вважатись шумом і НЕ починати/закінчувати сесію відстеження. Утримати явно більше 4 кадрів → має надійно довірятись, не через раз.

### 12. Межовий час: вікно подвійного щіпка
Вікно — 0.3с. Два швидкі щіпки з проміжком помітно МЕНШЕ 0.3с → один DOUBLE_PINCH. З проміжком помітно БІЛЬШЕ 0.3с → два окремі PINCH-кліки. З проміжком якомога ближче до рівно 0.3с → система стабільно (не випадково від разу до разу) обирає один із варіантів.

### 13. Межовий час: утримання toggle в Call
Вимога — 1.5с. Утримати явно МЕНШЕ 1.5с і відпустити → нічого не перемикається. Утримати явно БІЛЬШЕ → надійно перемикається. Змінити кількість пальців посеред утримання (напр. два, потім три, до завершення 1.5с для будь-якого) → таймер утримання має початись заново для нової кількості, а не зарахувати час, витрачений на стару, і жодне перемикання не повинно спрацювати від утримання, яке не витримало повні 1.5с на одній кількості пальців.

### 14. Швидке перемикання між середовищем і режимом
З активним Work швидко входити й виходити з режиму Flip кілька разів поспіль (голос/клавіатура/UI впереміш) → стан середовища (DND, відкриті застосунки) ніколи не зачіпається жодним переходом режиму; фінальний стан: Work й далі активне, режим не активний. З активним режимом швидко перемикати середовища (напр. Work → Study → Movie поспіль) → активний режим не турбується протягом усього цього, фінальне середовище — останнє запитане, з повністю виконаними його власними enter_actions (не перерваними наступним запитом посередині).

### 15. Toggle в Call-режимі, коли цільовий застосунок не запущений
Увійти в Call без запущеного Microsoft Teams взагалі, викликати toggle (напр. два пальці для камери) → система не падає і друкує звичайний рядок `[EXECUTOR] TOGGLE_CAMERA` — клавіша надсилається незалежно від того, чи Teams фронтмост/запущений, тож очікувано, що це тихо нічого корисного не робить, а не видає помилку.

### 16. Навантаження голосового розпізнавання під час активного відстеження жесту
Увійти в Flip, виконувати безперервну сесію відстеження свайпу (кулак розкритий, рука рухається), одночасно промовляючи голосову команду зі словом-активатором → голосова команда все одно розпізнається правильно (без втраченого звуку, без пропущеного слова-активатора) попри паралельну роботу циклу обробки камери, і саме відстеження жесту не має помітно "заїкатись" чи губити кадри, поки активне розпізнавання мовлення.

---

## gesture_test_scenarios.md

**Сценарії тестування розпізнавання жестів**

Ручні функціональні тест-сценарії для модуля жестів (`GestureRecognizer`, `GestureModel`/`OffHandModel`). Кожен сценарій оцінюється за тією самою триступеневою шкалою, що й у всій цій тест-сюїті:

- **Правильно** — очікуваний сигнал/дія спрацьовує надійно, з першої спроби, без сторонніх побічних ефектів.
- **Частково правильно** — потрібний сигнал зрештою спрацьовує, але лише після помітно невдалої першої спроби, неочікуваної затримки, або разом із незначним небажаним побічним ефектом (наприклад, не той режим на мить "блимає" перед тим, як самокоригується).
- **Неправильно** — спрацьовує не той сигнал, нічого не спрацьовує взагалі, або відбувається не той ефект на екрані.

Запустіть `python src/main.py --debug-gesture` для живого оверлею розпізнаного жесту, його впевненості та відстежуваних точок руки — використовуйте його, щоб підтвердити, *що саме побачив розпізнавач*, коли результат виглядає неправильним, перш ніж вирішувати, чи проблема в розпізнаванні, чи в подальшому мапінгу (`config/fusion.json`). Потрібна фізична камера й добре освітлена рука; ніщо з цього не можна автоматизувати headless.

### 1. Режим Flip — напрямні свайпи

Спершу увійдіть у режим Flip (голосом "flip mode", клавіатурою `ctrl+shift+f`, або через Quick Circle — див. §3). Для кожного рядка: стисніть кулак, розкрийте долоню (це починає відстежувану сесію), потім проведіть кінчиком вказівного пальця в заданому напрямку.

| # | Напрямок свайпу | Очікуваний сигнал | Очікувана дія |
|---|---|---|---|
| 1 | Вгору | `HAND_UP` | Прокрутка вмісту вниз |
| 2 | Вниз | `HAND_DOWN` | Прокрутка вмісту вгору |
| 3 | Вліво | `HAND_LEFT` | `FLIP_NEXT` |
| 4 | Вправо | `HAND_RIGHT` | `FLIP_PREVIOUS` |

Також перевірити:
- Повільний, навмисно нерізкий рух руки, що лишається нижче порогу швидкості, не має видавати жодного сигналу свайпу (відсіювання тремтіння/випадкового руху).
- Два свайпи, виконані один за одним швидше за вікно `motion_cooldown` (0.5 с) — переконатись, що другий ігнорується, а не ставиться в чергу.
- Повторне стискання кулака завершує відстежувану сесію (`HAND_SESSION_END`), і свайп, виконаний одразу після цього, нічого не робить, доки долоня знову не розкриється.

### 2. Режим Presentation — свайп кулаком

Увійдіть у режим Presentation. Стисніть кулак і рухайте **зап'ястям** (не лише кінчиком пальця) вліво/вправо — це окремий детектор на основі зап'ястя зі своїми порогами (налаштований під дистанцію презентування, далі від камери, ніж свайп у режимі Flip).

| # | Рух кулака | Очікувана дія |
|---|---|---|
| 1 | Вліво | `PREVIOUS_SLIDE` |
| 2 | Вправо | `NEXT_SLIDE` |

Примітка: на відміну від свайпу в режимі Flip, тут немає окремого жесту "увімкнути/вимкнути" — сам утримуваний кулак є активним станом. Переконайтесь, що розкриття долоні посеред руху скасовує жест (свайп не спрацьовує з уже розпочатого руху, який раптово стає розкритою долонею).

### 3. Quick Circle — вибір режиму

З стану бездіяльності (жоден режим не активний) виконайте жест початку сесії руки (стиснутий кулак → розкрита долоня), щоб викликати оверлей Quick Circle, потім проведіть у кожному напрямку.

| # | Напрямок свайпу | Очікуваний режим |
|---|---|---|
| 1 | Вліво | Presentation |
| 2 | Вправо | Call |
| 3 | Вгору | Flip |
| 4 | Вниз | Cursor |

Також перевірити:
- Quick Circle **не** відкривається, поки інший режим (наприклад, Flip) вже активний і в ньому триває сесія свайпу.
- Повторне стискання кулака без свайпу в жодному напрямку закриває коло без входу в будь-який режим.

### 4. Режим Cursor — курсор, клік, перетягування, правий клік

Увійдіть у режим Cursor. Вкажіть вказівним пальцем (`Pointing_Up`).

| # | Дія | Кроки | Очікуваний результат |
|---|---|---|---|
| 1 | Відстеження курсора | Рухайте вказівною рукою по кадру | Курсор на екрані слідує за кінчиком пальця; наближення близько до (не точно на) краю кадру камери досягає краю екрана |
| 2 | Клік | Швидкий щіпок (дотик великого й вказівного пальців, потім відпускання) | Одиничний клік спрацьовує *після* вікна подвійного щіпка (~0.3 с), якщо другий щіпок не пішов |
| 3 | Перетягування / скрол | Щіпок і утримання під час руху рукою, потім відпускання | Безперервний скрол під час утримання; відпускання **не** викликає також клік |
| 4 | Правий клік | Два швидкі щіпки поспіль | Спрацьовує один `DOUBLE_PINCH` (правий клік) — не два окремі кліки |

### 5. Режим Cursor — точність і наближення другою рукою

Все ще в режимі Cursor, з основною рукою в положенні `Pointing_Up`, внесіть другу руку в кадр.

| # | Жест другою рукою | Очікуваний результат |
|---|---|---|
| 1 | Друга рука стиснута (великий+вказівний торкаються), утримується | Рух курсора сповільнюється приблизно вдвічі (режим точності/"зниження DPI") відносно того самого фізичного руху руки без цього |
| 2 | Відпустити щіпок другою рукою | Курсор перестрибує на поточну абсолютну позицію кінчика основного пальця (очікуваний стрибок, не баг) |
| 3 | Друга рука розкрита, рухайте кінчиками обох вказівних пальців нарізно/разом (відстань щіпка основної руки змінюється, поки утримується щіпок другою рукою) | Активний застосунок наближається (розведення) / віддаляється (зведення) |

Також перевірити: активація zoom посеред кліку/перетягування основною рукою чисто скасовує його (жоден зайвий клік не спрацьовує після вимкнення zoom).

### 6. Режим Call — перемикачі за кількістю пальців

Увійдіть у режим Call. Для кожного рядка підніміть задану кількість пальців (великий палець не рахується) і утримуйте безперервно щонайменше 1.5 секунди.

| # | Піднято пальців | Очікувана дія |
|---|---|---|
| 1 | Один | Перемкнути мікрофон |
| 2 | Два | Перемкнути камеру |
| 3 | Три | Перемкнути звук дзвінка |
| 4 | Чотири | Перемкнути розмиття фону |

Також перевірити:
- Утримання менше 1.5 с **не** перемикає нічого.
- Показ тієї самої кількості пальців вдруге **без** того, щоб рука залишила кадр камери між спробами, **не** перемикає повторно (поведінка блокування) — рука має повністю залишити й повернутись у кадр спочатку.

### 7. Перевірка прив'язки до режиму (негативні тести)

Для кожної пари нижче переконайтесь, що жест **не** дає жодного ефекту (навіть неправильного), поки активний "чужий" режим:

| # | Жест | "Чужий" режим для перевірки | Очікуваний результат |
|---|---|---|---|
| 1 | Щіпок (дотик великого+вказівного) | Flip | Немає кліку, немає скролу |
| 2 | Кількість пальців (наприклад, два) | Cursor | Немає перемикання камери/мікрофона |
| 3 | Присутність другої руки | Flip або Presentation | Немає поведінки точності/наближення |

### Як тестувати

1. Запустіть `python src/main.py --debug-gesture`.
2. Увійдіть у режим, названий у кожному розділі (голосова команда, комбінація клавіш, або Quick Circle — див. таблицю вибору режиму вище).
3. Виконайте кожен жест і порівняйте вивід консолі/оверлею та фактичний ефект на екрані/в системі зі стовпцем "Очікуваний результат".
4. Занотуйте оцінку (правильно / частково правильно / неправильно) для кожного рядка, плюс короткий коментар для всього часткового чи неправильного (що сталося натомість).

---

## face_test_scenarios.md

**Сценарії тестування розпізнавання обличчя**

Ручні функціональні тест-сценарії для модуля обличчя (`FaceRecognizer`, `FaceModel`). Та сама триступенева шкала оцінки, що й у `gesture_test_scenarios.md`:

- **Правильно** — очікуваний сигнал/дія спрацьовує надійно, з першої спроби, без сторонніх побічних ефектів.
- **Частково правильно** — потрібний сигнал зрештою спрацьовує, але лише після неприродно перебільшеного руху, помітної затримки, або незначного небажаного побічного ефекту.
- **Неправильно** — спрацьовує не той сигнал, нічого не спрацьовує взагалі, або відбувається не той ефект на екрані.

Запустіть `python src/main.py --debug-face` для живого оверлею pitch/yaw/roll і смужки для кожного blendshape (брови/рот/моргання) з позначками порогів входу/виходу — використовуйте його, щоб побачити, чи межовий результат — проблема розпізнавання (значення так і не перетнуло поріг), чи проблема мапінгу (сигнал спрацював, але ніщо його не обробило). Потрібна фізична камера й добре освітлене обличчя; ніщо з цього не можна автоматизувати headless.

Усі сценарії нижче припускають, що система в стані **бездіяльності** (жоден режим жестів не активний) — відстеження обличчя повністю пригнічується, поки активний будь-який режим, див. §5.

### 1. Нахил голови (з утриманням Alt)

Утримуйте `alt`, потім нахиліть голову вбік (вухо до плеча, обличчя й далі повернуте до камери — це *roll*, а не поворот голови подивитись вліво/вправо, що є *yaw* і не дає жодного сигналу).

| # | Напрямок нахилу | Очікуваний сигнал | Очікувана дія |
|---|---|---|---|
| 1 | Вправо | `HEAD_TILT_RIGHT` | `NEXT_TRACK` |
| 2 | Вліво | `HEAD_TILT_LEFT` | `PREVIOUS_TRACK` |

Також перевірити:
- Відпускання `alt` і повторення того самого нахилу не дає **жодної** дії.
- Нахил спрацьовує лише один раз при вході в зону нахилу, не безперервно під час утримання.
- Повернення у вертикальне положення й повторний нахил знову коректно спрацьовує (гістерезис: вхід приблизно на 15°, вихід лише після повернення нижче ~8° — значення точно на межі не повинно "блимати" сигналом повторно).

### 2. Брови (з утриманням Alt / Ctrl)

| # | Утримуваний модифікатор | Дія | Очікуваний сигнал | Очікувана дія |
|---|---|---|---|---|
| 1 | `alt` | Підняти брови один раз | `EYEBROWS_UP` | `VOLUME_UP` (одна поділка) |
| 2 | `ctrl` | Підняти брови один раз | `EYEBROWS_UP` | `VOLUME_DOWN` (одна поділка) |
| 3 | немає | Підняти брови один раз | `EYEBROWS_UP` спрацьовує (лише консоль/оверлей) | Жодної дії — переконатись, що нічого не відбувається без утримуваного модифікатора |
| 4 | будь-який | Опустити брови назад | `EYEBROWS_DOWN` | Дія наразі не прив'язана — оцінювати за тим, чи спрацьовує сам сигнал, а не за ефектом |

Також перевірити: два підняття брів, завершені в межах ~0.6 с одне від одного, дають один сигнал `DOUBLE_EYEBROWS_UP` (лише консоль/оверлей — дія ще не прив'язана, так само як `EYEBROWS_DOWN`), а не дві окремі зміни гучності від `EYEBROWS_UP`.

### 3. Відкриття рота (з утриманням Alt)

| # | Утримуваний модифікатор | Дія | Очікуваний сигнал | Очікувана дія |
|---|---|---|---|---|
| 1 | `alt` | Відкрити рот | `MOUTH_OPEN` | Перемикається пауза/відтворення |
| 2 | немає | Відкрити рот | `MOUTH_OPEN` спрацьовує (лише консоль/оверлей) | Жодної дії — переконатись, що нічого не відбувається без `alt` |

Також перевірити: `MOUTH_OPEN` спрацьовує лише один раз, на передньому фронті — закриття рота знову **не** викликає повторного перемикання.

### 4. Подвійне моргання (з утриманням Alt) і кивок

| # | Утримуваний модифікатор | Дія | Очікуваний сигнал | Очікуваний результат |
|---|---|---|---|---|
| 1 | `alt` | Моргнути двічі швидко (в межах ~0.5 с) | `DOUBLE_BLINK` | Скріншот зберігається на робочому столі (чи в налаштованій теці) |
| 2 | `alt` | Лише одне моргання | немає сигналу | Переконатись, що нічого не спрацьовує від одного моргання |
| 3 | немає | Кивнути головою (швидко вниз-вгору чи вгору-вниз) | `CONFIRM` спрацьовує (лише консоль/оверлей) | Дія наразі не прив'язана — оцінювати за тим, чи спрацьовує сигнал, а не за видимим ефектом |

### 5. Прив'язка лише до стану бездіяльності (негативний тест)

Увійдіть у будь-який режим (наприклад, Flip, голосом чи клавіатурою), потім, поки він активний:

| # | Дія | Очікуваний результат |
|---|---|---|
| 1 | Нахилити голову, як у §1, з утриманим `alt` | Жодного сигналу `HEAD_TILT_*`, жодної зміни треку |
| 2 | Відкрити рот з утриманим `alt` | Жодного сигналу `MOUTH_OPEN`, жодної паузи/відтворення |
| 3 | Кивнути головою | Жодного сигналу `CONFIRM` |

Вийдіть з режиму (назад у бездіяльність) і переконайтесь, що сигнали обличчя одразу відновлюють роботу без потреби перезапускати застосунок.

### Як тестувати

1. Запустіть `python src/main.py --debug-face`.
2. Зі стану бездіяльності пройдіть §1–§4 по порядку, утримуючи модифікатор (`alt`/`ctrl`), названий у кожному рядку, перед виконанням мімічного руху.
3. Порівняйте вивід консолі/оверлею та фактичний ефект на рівні системи (чи його відсутність, для зарезервованих сигналів без дії) зі стовпцями "Очікувано".
4. Потім увійдіть у режим і повторіть кілька тих самих рухів за §5, щоб перевірити прив'язку лише до бездіяльності.
5. Занотуйте оцінку (правильно / частково правильно / неправильно) для кожного рядка, плюс короткий коментар для всього часткового чи неправильного.

---

## stress_test_scenarios.md

**Стрес-тести / edge-case сценарії**

Ручні стрес- та edge-case сценарії для працюючої системи. На відміну від `gesture_test_scenarios.md` і `face_test_scenarios.md`, тут оцінка **бінарна** (pass/fail), а критерій — **стабільність системи**, не точність розпізнавання: чи лишається система в узгодженому, відповідному стані після сценарію, без збою й без назавжди застряглого режиму?

Ці сценарії існують саме для випадків часу/паралелізму, які автоматизована регресна сюїта (`test_command_pipeline.py`) покрити не може — та сюїта прожене конвеєр синтетичними, по одному, подіями `EventBus`, тож вона вже повністю покриває, чи правильна сама *логіка рішень* для заданого входу (наприклад, що `exit_actions` середовища виконуються перед `enter_actions` наступного). Що вона покрити не може — це те, що відбувається, коли реальне обладнання видає сигнали з реальним, перекривним часом — саме для цього існує цей документ. Потрібні фізична камера, мікрофон і клавіатура; ніщо з цього не можна автоматизувати headless.

- **Pass** — система лишається повністю відповідною після цього: досягається правильний (або принаймні розумний, несуперечливий) кінцевий стан, у логі/консолі не видно жодного винятку, і жоден подальший ввід не ігнорується назавжди.
- **Fail** — застосунок падає, стає невідповідним, застряє в режимі, який більше не реагує на запити виходу, або опиняється у видимо суперечливому стані (наприклад, UI показує один активний режим, поки система насправді в іншому).

### 1. Швидке перемикання режимів

Викликайте зміни режиму одну за одною, швидше, ніж людина зазвичай робить паузу між ними — наприклад, скажіть "flip mode", потім одразу "cursor mode", потім одразу "call mode", усе в межах пари секунд (у межах `signal_timeout`, 2 с).

- Переконайтесь, що фінальний активний режим — останній запитаний, а не якийсь попередній, "застряглий" посеред переходу.
- Переконайтесь, що UI (світлове кільце на колесі керування / плаваюча статус-панель) узгоджується з фактичним активним режимом у кожен момент, не лише в кінці.
- Повторіть, використовуючи комбінації клавіш (`ctrl+shift+f`, `ctrl+shift+c`, `ctrl+shift+w`, `ctrl+shift+p`), натиснуті швидко одна за одною, замість голосу.

### 2. Одночасні сигнали кількох модальностей

Створіть два сигнали з різних модальностей у (максимально близько до) один момент — наприклад, скажіть "cursor mode" точно в момент, коли також виконуєте жест свайпу Quick Circle, або утримуйте комбінацію клавіш для входу в режим, одночасно промовляючи голосову команду іншого режиму.

- Переконайтесь, що система детерміновано розв'язується до одного результату (не намагається увійти в два режими одночасно, не падає від конфліктного вводу).
- Переконайтесь, що рядок `[RESOLVED]` у лозі називає рівно одне джерело причиною, що відповідає тому, на чому система фактично зупинилась.

### 3. Швидке відкриття/закриття Quick Circle

Виконайте жест початку сесії руки (кулак → розкрита долоня) і одразу знову стисніть кулак, кілька разів поспіль, жодного разу не проводячи свайп у якомусь напрямку.

- Переконайтесь, що коло щоразу чисто відкривається й закривається без залишкового оверлею на екрані.
- Переконайтесь, що жоден режим не входить випадково лише від цього.
- Потім повторіть, але при одній з повторень дійсно проведіть свайп для вибору режиму — переконайтесь, що саме ця спроба все одно коректно входить у обраний режим, попри швидке відкриття/закриття безпосередньо перед нею.

### 4. Утримуваний модифікатор під час незв'язаних сигналів

Утримуйте `alt` безперервно. Поки утримуєте, послідовно: виконайте незв'язаний жест (наприклад, свайп у режимі Flip), промовте незв'язану голосову команду, потім нарешті виконайте дію face-layer, яку має вмикати alt (наприклад, нахил голови для наступного треку).

- Переконайтесь, що дія face-layer (наступний трек) спрацьовує рівно один раз, не повторно весь час, поки `alt` утримується, і не заднім числом для попередніх, незв'язаних жесту/голосу сигналів, які відбулись, поки alt уже утримувався.

### 5. Вимкнення системи посеред режиму

Увійдіть у будь-який режим (наприклад, Presentation), потім клацніть центральну кнопку системи ON/OFF в UI, щоб вимкнути систему, поки режим ще активний.

- Переконайтесь, що режим видимо виходить (світлове кільце вимикається) як частина вимкнення, а не лишається активним-але-невідповідним.
- Увімкніть систему знову — переконайтесь, що жоден режим тихо не лишився активним з попереднього стану, і що голосові/жестові/клавіатурні команди знову працюють нормально.

### 6. Вимкнення вхідного модуля посеред використання

Активно використовуючи режим, що залежить від заданого вводу (наприклад, Cursor, якому потрібна камера), скористайтесь перемикачем нижньої статус-панелі, щоб вимкнути цей модуль (камеру) посеред використання.

- Переконайтесь, що активний режим не падає; перегляд камери має знову показати заглушку, а рух курсора жестами — просто зупинитись, без видимої помилки.
- Увімкніть модуль знову й переконайтесь, що він відновлює роботу без потреби перезапускати застосунок чи заново входити в режим.

### 7. Швидкі кліки в UI

Клацніть кожну з чотирьох кнопок колеса режимів у головному вікні швидко поспіль, швидше, ніж їхні реальні жестові/голосові еквіваленти могли б колись бути видані.

- Переконайтесь, що фінальний активний режим відповідає останній натиснутій кнопці.
- Переконайтесь, що підпис "Current mode: …" і світлове кільце ніколи не розходяться, навіть на мить під час клацання.

### 8. Рука залишає кадр під час утримання toggle-жесту в Call-режимі

У режимі Call підніміть кількість пальців для перемикання (наприклад, два, для камери) і, посеред необхідного утримання 1.5 с, ненадовго виведіть руку з кадру й поверніть назад до завершення утримання.

- Переконайтесь, що коротке випадання (кадр-два, наприклад через розмиття від руху) не скидає таймер утримання так, щоб жест ставав практично неможливим завершити.
- Переконайтесь, що рука, яка *повністю* залишає кадр на тривалий час, дійсно скидає утримання, вимагаючи показати жест знову спочатку (це задокументована, правильна поведінка — перевірте, що вона досі діє, а не відсутня).

### 9. Регресна перевірка запакованого `.app`: жодного дублювання процесів

Це цільова регресна перевірка конкретного історичного бага (див. главу 4.4 тези): після пакування воркери `ProcessPoolExecutor`, що використовуються для резервного розпізнавання Whisper / семантично-LLM зіставлення, могли самі повторно запускати весь застосунок замість старту як звичайні робочі процеси, оскільки `sys.executable` вказував на сам запакований застосунок.

- Запустіть запакований `.app` (не `python src/main.py`).
- Навмисно викличте резервне розпізнавання Whisper кілька разів поспіль (промовте фрази, розраховані на промах граматики Vosk — див. фрази рівня 3 з `voice_test_phrases.md`), і окремо викличте резервне семантичне/LLM зіставлення команд кілька разів.
- Після кожної серії перевірте Activity Monitor (або `ps aux | grep GestureVoiceControl`) — переконайтесь, що працює рівно один екземпляр застосунку, а не по одному новому на кожен викликаний резервний механізм.

### 10. Тривала перевірка в стані бездіяльності

Залиште застосунок працювати в стані бездіяльності (камера й мікрофон активні, жоден режим не введений) щонайменше на 30 хвилин.

- Переконайтесь, що він все ще відповідає на голосову команду чи жест наприкінці цього періоду.
- Примітка: система наразі не має процесу нагляду/автовідновлення (див. главу 4.4, "відоме обмеження") — цей сценарій призначений для спостереження, чи ця прогалина фактично викликає практичну проблему протягом реалістичної тривалості сесії, а не для перевірки механізму відновлення, якого не існує.

### Як тестувати

1. Запустіть `python src/main.py` (сценарій 9 конкретно вимагає запакований `.app` замість цього — див. той розділ).
2. Пройдіть кожен сценарій вище, спостерігаючи і за поведінкою на екрані, і за виводом консолі/логу на предмет винятків чи рядків `[RESOLVED]`/`Mode ->`, які не відповідають очікуваному.
3. Занотуйте pass/fail для кожного сценарію, з коротким коментарем про те, що фактично сталося для будь-якого fail (які рядки логу з'явились, у якому стані лишився UI).

---
